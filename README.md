## Aim-Bot Detection code ##

To run the code execute the following
```
python aimbot.py --prototxt MobileNetSSD_deploy.prototxt.txt --model MobileNetSSD_deploy.caffemodel
```

Keybinding:
q - quits program
